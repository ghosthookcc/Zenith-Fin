generator client {
  provider = "prisma-client"
  output   = "../../generated/prisma"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "../uml/erd.svg"
}

datasource db {
  provider = "postgresql"
}
